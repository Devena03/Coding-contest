<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contest App</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .navbar-custom {
            background-color: #219C90;
        }
        .navbar-custom .navbar-brand,
        .navbar-custom .navbar-text {
            color: #ffffff;
        }
        .btn-custom {
            background-color: #219C90;
            border-color: #219C90;
            color: #ffffff;
        }
        .btn-custom:hover {
            background-color: #1a7e71;
            border-color: #1a7e71;
            color: #ffffff;
        }
        .footer {
            background-color: #219C90;
            color: #ffffff;
            padding: 20px 0;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
            height: 8px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark navbar-custom">
        <a class="navbar-brand" href="#">Contest App</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto"></ul>
        </div>
    </nav>

    <div class="container">
        <h1 class="mt-5 mb-4">Upcoming Contests</h1>
        <div class="row" id="contestCards">
            <% contests.forEach(contest => { %>
                <div class="col-md-3 mb-4">
                    <div class="card">
                        <% 
                            const randomImageNumber = Math.floor(Math.random() * 4) + 1;
                            const imageUrl = `/images/${randomImageNumber}.jpg`;
                        %>
                        <img src="<%= imageUrl %>" class="card-img-top" alt="Contest Image">
                        <div class="card-body">
                            <h5 class="card-title"><%= contest.event %></h5>
                            <p class="card-text">
                                <strong>Start Time:</strong> <%= contest.start %><br>
                                <strong>Platform:</strong> <%= contest.resource %><br>
                            </p>
                            <a href="<%= contest.href %>" class="btn btn-custom stretched-link" target="_blank">Visit Contest</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <footer class="footer">

            <p>&copy; Deverma - <%= new Date().getFullYear() %></p>

    </footer>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
